{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "Clean",
            "command": "rm -rf build",
            "args": [],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": []
        }
    ]
}
